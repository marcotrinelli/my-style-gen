# {{topic_title}}

{{one_sentence_on_when_to_read_this}}

## Rules

{{rules}}

## Weaker signal

Each of these was seen once rather than corroborated, so it may reflect the constraints
of one setting rather than a general preference. Follow it by default; drop it without
ceremony where the context points elsewhere.

{{single_source_rules}}
